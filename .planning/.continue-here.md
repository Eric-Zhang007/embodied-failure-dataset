---
context: default
phase: m1-runtime-debug
task: 7
total_tasks: 7
status: complete
last_updated: 2026-07-15T22:00:00.000Z
---

# Continue Here — M1: System Stabilization + Semantic Memory

## Current State

M1 runtime-debug phase complete. All 7 tasks done. E2E validated on pick_and_place_simple (6 steps + 19 steps resume). 58/58 tests. All planning docs refreshed and pushed to GitHub.

Key deliverables:
- **Semantic memory**: 3-layer architecture (interface + geometric + semantic), receptacle-grouped, freshness-aware, task-tracking, first-person "I" perspective
- **VLM hardening**: HTTP-200 5-stage validation, reasoning_effort auto-strip fallback, 400 retry, provider migration to 9527code.com
- **Scheduler**: per-worker agent isolation, fixed accounting, 9 tests
- **E2E**: task_complete validated (SoapBottle→Toilet 6 steps, Book→Desk 19 steps)
- **Docs**: CLAUDE.md, PROJECT.md, STATE.md, ROADMAP.md, REQUIREMENTS.md, ARCHITECTURE.md, STACK.md, CONCERNS.md, MILESTONE_SUMMARY-vM1.md all refreshed

## Blocking Constraints

- [ ] **gpt-5.5 content=None**: The model intermittently returns only reasoning_content with no content. Mitigated with 5+3 retries + token scaling, but ~10% of 1-image calls still exhaust. Plan accordingly.
- [ ] **Tomato/Apple visual confusion**: The model cannot distinguish red round objects. Semantic memory has the "WHERE MY TARGET IS" section to help, but the Planner currently ignores it.

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Planner ignores memory | Planner sees "Apple last seen inside Fridge 63 steps ago" but still outputs "approach CounterTop" because a red Tomato is visible | blocking | P2 integration: inject explicit instruction "If target is remembered inside a container, OPEN that container first" |
| Static review as gate | Expensive MAX multi-agent review was slow while E2E immediately found bugs | advisory | Focused regression → real E2E → inspect → fix |

## Completed Work

- T1-T7: All runtime-debug tasks done (see HANDOFF.json for details)
- Semantic memory module creation (semantic_memory.py, geometric_memory.py, memory_interface.py)
- Provider migration fullcupai → 9527code
- reasoning_effort xhigh→medium default
- System prompt shift to first-person "I"
- Camera horizon epsilon fix
- 4-direction scan metadata fix
- PickupObject Priority 4 removal
- 58 unit tests passing

## Remaining Work (P2-P6)

1. **P2 integration**: Make Planner USE semantic memory's "WHERE MY TARGET IS" — when it says "Apple last seen inside Fridge", output "open Fridge" not "approach CounterTop"
2. **P3**: Fuzzy intent decomposition (locate X → approach <location>)
3. **P4**: Systematic exploration + scan→scan fix
4. **P5**: Fork validation with E2E tests
5. **P6**: 7-task baseline (5 episodes each)

## Decisions Made

- Default reasoning_effort=medium for all gpt-5.5 (xhigh multi-image broken)
- 3-layer memory architecture with `--memory` CLI flag
- First-person "I" for all system prompts
- API provider: www.9527code.com/v1
- PickupObject Priority 4 removed from action_adapter

## Required Reading

1. `.planning/ROADMAP.md` — remaining phases P2-P6
2. `src/semantic_memory.py` — new memory format the Planner needs to leverage
3. `src/vlm_client.py:_chat_openai()` — retry/validation logic
4. `.planning/reports/MILESTONE_SUMMARY-vM1.md` — full milestone overview
5. `.planning/STATE.md` — current known issues

## Next Action

Start P2 Planner-level integration: add explicit rules to PLANNER_SYSTEM that instruct the Planner to check "WHERE MY TARGET IS" section of semantic memory before proposing intents. When target is remembered inside a container, prioritize opening that container.
