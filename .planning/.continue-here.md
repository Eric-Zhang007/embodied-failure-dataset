---
context: default
phase: m1-p2
task: 5
total_tasks: 6
status: in_progress
last_updated: 2026-07-17
---

# Continue Here — M1 P2: Memory Integration + Executor Escape + Phase 3 Fixes

## Current State

P2 integration + executor escape + memory freshness + Phase 3 hallucination fix all implemented. E2E test was at heating stage (Egg in Microwave, toggled on) when API got 524 timeout. 24 steps, 3 failures (12.5% — down from 50%).

## Blocking Constraints

- [ ] **CONSTRAINT: Phase 3 hallucination fix is prompt-only** — The model was proposing `CloseObject(Oven)` because the error message said `OVENDOOR.001 is blocking`. Fix: added `CRITICAL — COLLISION ENTITIES ARE NOT INTERACTABLE OBJECTS` block to `PHASE3_SYSTEM` (eb_agent.py:267). This is UNTESTED. Do NOT add fallback code — user explicitly rejected that approach. The prompt must teach the model.
- [ ] **CONSTRAINT: Tomato/Apple visual confusion is mitigated by P2 rules but untested** — The MEMORY OVER VISUAL GUESSING / CONTAINER-FIRST / DISAMBIGUATION rules should prevent the Planner from targeting a Tomato on CounterTop when memory says Apple is in Fridge. Needs an episode with both objects to verify.

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Silent replay skip | `replay_steps` silently skips PickupObject when objectId resolution fails, causing resume to lose inventory state | advisory | Added logging. Fix 1 (inventory restore) bridges the gap. |
| Executor overcommits in tight spaces | MoveRight x8, MoveAhead x10 proposed from constrained positions cause repeated dead_loop | advisory | Lateral cap x4, escape x8 with no re-approach. |

## Completed Work

### P2: Memory Integration (`src/eb_agent.py`)
- PLANNER_SYSTEM: MEMORY OVER VISUAL GUESSING, CONTAINER-FIRST, DISAMBIGUATION rules
- EXPLORE_BIAS_SYSTEM: same rules, exploration subordinate to memory

### Executor Escape Fix (`src/executor.py`)
- STUCK ESCAPE: MoveBack x8 (was x4), no re-approach after rotate
- Lateral cap: MoveRight/MoveLeft repeat <= 4

### Memory Freshness (`src/branch_runner.py` + `src/semantic_memory.py`)
- Fix 1: Inventory restore on resume (parses episode steps for held object, re-picks up)
- Fix 2: Replay fidelity logging (warns on skipped PickupObject/MoveSequence)
- Fix 3: Intent passed to all `memory.update()` calls (8 sites)
- Fix 4: `_render_remembered_target_info` appends saturation signal after N failures
- Fix 5: `_detect_phase` overrides to EXPLORATION after 4+ interaction failures

### Phase 3 Fix (`src/eb_agent.py`)
- PHASE3_SYSTEM: CRITICAL block about collision entities (Cube.001, OVENDOOR.001, etc.) not being interactable

### Cleanup
- Removed 15 stale debug/inspect scripts

## Remaining Work

1. **Verify task completion**: Re-run `pick_heat_then_place_in_recep` E2E — was at heating stage
2. **P3**: Fuzzy intent decomposition
3. **P4**: Systematic exploration + scan->scan fix
4. **P5**: Fork validation
5. **P6**: 7-task baseline

## Decisions Made

- Phase 3 fix: prompt-only (user rejected fallback code in branch_runner)
- Executor escape: MoveBack x8 minimum, no re-approach in escape sequence
- Lateral cap: MoveRight/MoveLeft capped at repeat 4
- Memory freshness: 5 coordinated fixes targeting replay fidelity + stuck tracker

## Required Reading

1. `src/eb_agent.py:267` — PHASE3_SYSTEM with collision-entity rules (read the CRITICAL block)
2. `src/executor.py:21-22` — updated STUCK ESCAPE + lateral cap rules
3. `src/semantic_memory.py:264-320` — `_render_remembered_target_info` with saturation signal + `_detect_phase` with failure override
4. `.planning/ROADMAP.md` — remaining P3-P6

## Next Action

```bash
wsl bash -c "source ~/.profile; cd ~/embodied-failure-dataset && uv run python scripts/e2e_test.py --api-key sk-IxXjSiRZ3IhCd4hxMajweiXamF0R1U1cHmNECBrRbIz8v0hy --task pick_heat_then_place_in_recep --random --no-traps --memory semantic"
```

Agent was at: Egg picked up → placed in Microwave → Microwave toggled on → waiting to retrieve heated Egg and place on CounterTop. Should finish in ~10-15 more steps.
