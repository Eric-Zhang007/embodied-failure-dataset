---
context: default
status: paused
last_updated: 2026-07-14T23:30:00+08:00
---

# Continue Here — Embodied Failure Dataset

## Current State

Two commits on `main`:
- `0995707` — 6 navigation spikes + 8 bugfixes + ablation framework + tree history
- `4ce70a9` — StuckTracker integration from PR #2

## Completed Work

### Bugfixes (8/8)
All verified working in e2e tests:
- B1: 5xx/524 retry ✅
- B2: AABB surface distance ✅  
- B3: CoT capture (default on, graceful fallback) ✅
- B4: Sliced naming ✅
- B5: Intent history diagnosis (fixed + re-fixed after audit) ✅
- B6: Incomplete criteria text ✅
- B7: Stale spatial memory ✅
- B8: failed_object_ids ✅

### Navigation Spikes (6/6 implemented, 0 quantified)
- 001: searched-markers (instance-level + unmark)
- 002a: intent-dedup (escalating: warn→block)
- 002b: critic-guard (3 rules + optional LLM, default off)
- 003: curiosity-scoreboard (3D proactive scoring)
- 004: contrastive-planner (gated dual-Planner)
- 005: progress-gating (5-phase prompt injection)
- 006: search-trail-cost (0.5m grid soft scoring)

### Infrastructure
- Tree-structured intent history (Planner sees intent tree, Executor sees current intent)
- Ablation framework (per-spike flags + `--ablation` CLI + `scripts/analyze_ablation.py`)
- CoT always saved to api_calls.jsonl
- StuckTracker: cumulative intent failure tracking (from PR #2)

### Documentation
- `.planning/spikes/MANIFEST.md` — complete experiment tracking
- `.planning/reports/MILESTONE_SUMMARY-vM1.md` — full session summary
- 7 spike READMEs

## Remaining Work

### Blocking: E2E completion
No task has been successfully completed yet. The root causes are in the EXECUTOR layer, not the Planner:

1. **Empty MoveSequence loop**: Executor outputs `actions: []` with `status: "done"` when already at target → Phase 3 diagnoses → recovery → same thing again
2. **Camera pitch exhaustion**: Executor repeatedly calls LookDown until -60° limit, doesn't know to LookUp
3. **Semantic prior table too sparse**: 99 entries, HandTowel scored below Floor
4. **Teleport corner traps**: Some FloorPlan scenes spawn agent in geometrically trapped positions

### Ablation experiments
Need to run controlled experiments with different spike combinations and examine api_calls.jsonl:
```bash
# Baseline (all spikes off)
uv run python scripts/e2e_test.py --task pick_and_place_simple --random --no-traps \
  --ablation 001 002a 003 004 005 006

# Each spike individually  
uv run python scripts/e2e_test.py --task pick_and_place_simple --random --no-traps \
  --ablation 002a 002b 003 004 005 006  # only 001
# ... etc
```

### PR #2 pending
laoma9604-design's StuckTracker is integrated. The dead loop threshold changes were rejected. PR can be closed with comment.

## Key Decisions
- All spikes default ENABLED for maximum defense
- 002b (critic-guard) defaults OFF (adds latency)
- Tree history replaces JSONL dump — simpler, more readable
- StuckTracker's cumulative tracking > our 002a windowed approach
- E2E verification blocked by Executor issues, not Planner

## Files Modified (uncommitted)
None — all changes committed in 0995707 and 4ce70a9.

## Next Action
1. Fix Executor empty-actions loop
2. Fix camera pitch awareness (camera_horizon already passed to execute_intent!)
3. Run ablation experiments
4. Quantify spike contributions
