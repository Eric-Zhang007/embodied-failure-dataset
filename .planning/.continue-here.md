---
context: default
phase: runtime-debug
task: 6
total_tasks: 6
status: paused
last_updated: 2026-07-15T12:00:00.000Z
---

# Continue Here — Real E2E + Parallel Pipeline Debugging

## BLOCKING CONSTRAINTS — Read Before Anything Else

> These constraints were discovered through actual failures. Acknowledge them before proceeding.

- [ ] **Preserve dirty work** — The repository contains extensive pre-existing modified, mode-only, and untracked files. Do not reset, clean, checkout, remove `.claude/worktrees/wf_55c0ecf5-99b-1`, or broadly overwrite files.
- [ ] **Real E2E beats MAX review here** — The user explicitly rejected expensive maximum static review as a gate. Use focused tests, then aggressive real runs and evidence inspection.
- [ ] **Strict API/model failures** — Non-JSON or malformed HTTP-200 completions get bounded logged retries, then raise. Never create a fallback action, fake success, or route an API/model-contract failure into simulator Phase 3/4.
- [ ] **Use the correct endpoint** — `https://api.fullcupai.com` returns the web UI HTML at `/chat/completions`; use `https://api.fullcupai.com/v1` and fix every stale default.
- [ ] **Scene restoration must be proved semantically** — A successful `SetObjectPoses` call is insufficient. Check the target object's actual position/parent receptacle and the task predicate after restoration.
- [ ] **Inspect pictures** — For every real navigation/scene result, inspect `s*.png` and all LookAround views in addition to API/episode/failure logs.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Static review as runtime gate | MAX multi-agent review was slow and costly while actual E2E immediately found endpoint and restoration bugs | blocking | Focused regression → real E2E → inspect evidence → minimal fix |
| Exact ALFRED object-name matching | ALFRED 2.1 `Laptop_b256e6c5` did not equal AI2-THOR 5 `Laptop_86a40811`, so the target retained its default pose on Desk and the task auto-completed falsely | blocking | Match exact names first, then unmatched same-type instances by nearest source/target position; verify parent receptacles |
| Trusting HTTP 200 shape | Wrong endpoint returned HTML with status 200; later provider response had `message` without `content`, causing raw JSONDecodeError/KeyError | blocking | Validate JSON → choices → message → non-empty string content, log each malformed response, bounded retry, dump+raise |
| Shared mutable agents in parallel workers | `Scheduler` shares one `EBAgent` and one `BranchRunner`; both carry per-episode mutable state (`dedup_stats`, pending warnings, phase, `_last_scan_step`) | blocking | Isolate agents/runner per worker before trusting `--parallel > 1` |
| Surface-only task_complete | A two-step LookAround→Done run appeared successful only because restoration left Laptop on Desk | advisory | Compare trajectory high-level plan, restored metadata, task predicate, and pictures before counting coverage |

<current_state>

Real E2E is now the primary debugging method. The strict Executor/action-boundary changes passed focused 13/13, full 35/35, and py_compile before this session's new ALFRED fix.

Three E2E attempts established:

1. Without `/v1`, warmup and Planner failed because `https://api.fullcupai.com/chat/completions` returned HTTP-200 HTML.
2. With `/v1`, `trial_T20190909_074933_198388` reported `task_complete` in two steps (`LookAround → Done`). This was suspicious and has now been explained.
3. Another action-path probe reached the correct API but the first Planner image response had no `message.content`, causing `KeyError: 'content'` at `src/vlm_client.py:347`.

The initial completion was a confirmed scene-restoration bug. ALFRED says the Laptop starts on Bed and must be put on Desk. The live runtime object suffix differed, exact name matching failed, and the default AI2-THOR Laptop was already on Desk. `src/alfred_scene.py` now falls back from exact name to unused same-type pose matched by nearest position. A new regression is in `tests/test_alfred_scene.py`. A real WSL metadata probe now reports:

- completion: `False, 'Laptop must be inside Desk'`
- Laptop position `(2.6038, 0.7749, -1.056)`
- Laptop parent: Bed
- Desk contains no Laptop

The pose test suite passes 5/5. Full discovery has not yet been rerun after this new change.
</current_state>

<completed_work>

- Loaded and reconciled the previous runtime-debug checkpoint against live source.
- Verified strict Executor/action boundary changes in WSL: focused 13/13, full 35/35, py_compile passed.
- Stopped unnecessary MAX review after user feedback.
- Ran real no-trap E2E and diagnosed the missing `/v1` default.
- Inspected successful and failed episode JSON, API traces, and LookAround pictures.
- Proved initial `task_complete` was not valid ALFRED initial state; it was restoration drift.
- Fixed ALFRED-to-runtime object pose identity mismatch in `src/alfred_scene.py`.
- Added `test_restore_matches_alfred_pose_by_type_when_runtime_suffix_changes`.
- Verified the actual Laptop task restores Laptop on Bed and starts incomplete.
- Inspected `scripts/run_pipeline.py` and `src/scheduler.py`; identified untested parallel hazards and misleading stats.
</completed_work>

<remaining_work>

1. **VLM HTTP-200 response hardening**
   - Patch `src/vlm_client.py:_chat_openai`.
   - For each 200 response validate JSON object, non-empty `choices`, dict `message`, and non-empty string `content`.
   - Log malformed response body/JSON per attempt to the selected run's `api_calls.jsonl`.
   - Retry at most three transport-level attempts with small backoff; then `_dump_failure` and raise an actionable exception.
   - Preserve `reasoning_content` for logs, but do not treat reasoning-only output as the final answer unless the provider contract explicitly requires that and is verified.
   - Add mocked tests for HTML-200, missing content, and recovery on a later valid response.
2. **Fix endpoint defaults everywhere**
   - `scripts/e2e_test.py`
   - `scripts/run_pipeline.py`
   - `scripts/e2e_test_output.py`
   - `src/scheduler.py:SchedulerConfig`
   - `config.toml`
   - All should use `https://api.fullcupai.com/v1`.
3. Rerun focused + full WSL tests and py_compile after the above.
4. Rerun the known Laptop episode or another no-trap `pick_and_place_simple` until Executor/MoveSequence actually executes. Inspect API log, episode/failure log, main images, and all LookAround images.
5. **Parallel scheduler debug**
   - Do not launch broad parallel production yet.
   - `Scheduler` currently shares `EBAgent`, `ExecutorAgent`, `OracleAgent`, and `BranchRunner` across worker threads.
   - `EBAgent` has mutable per-episode state; `BranchRunner` has `_last_scan_step`; isolate these per worker.
   - Fix accounting: a worker exception increments `failed` and then also `completed`; skipped results are also counted completed; `_report.remaining` uses the never-drained main queue.
   - Ensure `_run_branch` legacy path passes `executor_agent` if retained.
   - Add deterministic scheduler tests.
   - Then run `scripts/run_pipeline.py --max 2 --parallel 2 --no-fork --api-base-url https://api.fullcupai.com/v1` and inspect both episodes/artifacts/pictures.
6. Only after two-worker main branches behave correctly, test forks serially.
</remaining_work>

<decisions_made>

- Prefer aggressive real execution over expensive broad static review once focused tests pass.
- Keep strict failure semantics: model/API contract errors never become environment failures or fallback behavior.
- Treat task-complete hard checks as trustworthy only after proving scene restoration matches the ALFRED initial state.
- Fix root identity mapping in scene restoration rather than special-casing the Laptop task.
- Debug parallel main branches before fork orchestration.
</decisions_made>

<blockers>

- HTTP-200 malformed/missing-content responses currently crash without useful bounded retries.
- Parallel Scheduler is not thread-safe because mutable agents/runner are shared.
- Working tree is very dirty; only minimal target edits are safe.
</blockers>

## Required Reading (in order)

1. `.planning/HANDOFF.json` — machine-readable current state and file inventory.
2. This file — exact runtime evidence, constraints, and next action.
3. `src/vlm_client.py:309-401` — HTTP client loop requiring strict response-shape handling.
4. `src/alfred_scene.py:34-175` — newly fixed pose restoration; preserve it.
5. `tests/test_alfred_scene.py` — new mismatched-suffix regression.
6. `output_e2e_retry_v1_20260715_1410/trial_T20190909_074933_198388.json` and its images — false surface success evidence.
7. ALFRED source trajectory: `data/json_2.1.0/train/pick_and_place_simple-Laptop-None-Desk-306/trial_T20190909_074933_198388/traj_data.json` — high-level plan proves Laptop starts on Bed.
8. `output_e2e_action_probe_20260715_1420/trial_T20190908_122018_917803.json` and images — correct-looking bathroom restoration; API failed before action execution.
9. `scripts/run_pipeline.py` and `src/scheduler.py` — parallel orchestration target.
10. `.planning/debug/executor-empty-action-loop.md` — prior strict-control-flow rationale.

## Infrastructure State

- No E2E, pipeline, simulator, API, or workflow background processes are running.
- WSL2 Ubuntu is required. Use `wsl.exe -d Ubuntu -- bash -lc 'cd /home/zjc/embodied-failure-dataset && ...'` from this harness.
- Correct API base URL: `https://api.fullcupai.com/v1`.
- Output evidence directories:
  - `output_e2e_20260715_135628/` — wrong endpoint, HTML-200 failure.
  - `output_e2e_retry_20260715_1402/` — repeated wrong-endpoint failure.
  - `output_e2e_retry_v1_20260715_1410/` — false surface success caused by restoration mismatch.
  - `output_e2e_action_probe_20260715_1420/` — correct endpoint, missing-content response before actions.
- Do not remove `.claude/worktrees/wf_55c0ecf5-99b-1`.

<context>

The user wants courage and aggressive runtime testing, not over-cautious MAX review. The correct loop is now: focused regression → real E2E → inspect text and pictures → minimal root-cause fix → repeat. The next blocker is small and concrete: harden the HTTP-200 response shape, fix `/v1` defaults, then rerun the actual action path. Once single-episode behavior is proven, isolate scheduler worker state and run exactly two parallel main branches.
</context>

<next_action>

Start with: patch `src/vlm_client.py:_chat_openai` for bounded, logged retries on HTTP-200 non-JSON/missing-content responses; add mocked regressions; update all fullcup defaults to `/v1`; run the full WSL test suite. Then perform the real action-path E2E and inspect pictures before touching parallel orchestration.
</next_action>
