---
context: default
phase: m1-p2
task: 5
total_tasks: 6
status: in_progress
last_updated: 2026-07-17T21:05:54+0800
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> Each constraint below was discovered through failure this session. Acknowledge before proceeding.

- [ ] **CONSTRAINT: Verify against the rendered frame + AI2-THOR ground truth, NOT the text stream.** This session I diagnosed the resume failure as "replay-fidelity bug" from a `PutObject skipped` warning + a quick frame read — and was WRONG. An AI2-THOR `env.get_state_snapshot()` probe proved the replay was FAITHFUL (Egg was `temperature:Hot` inside the closed Microwave). The real bug was pose/interaction-visibility. Mitigation: for any failure diagnosis, write a throwaway probe that replays and prints real object metadata (parentReceptacles, temperature, visibleBounds2D, inventoryObjects) before theorizing. `eb_reasoning`, replay warnings, and a single frame can all mislead.
- [ ] **CONSTRAINT: When probing the flaky provider API, use a FULL-SIZE image payload.** A tiny synthetic image probe returns HTTP 200 while the real 143KB multimodal Planner request 524s. Gating relaunch on a small probe re-wedges the run. Probe with a ~300x300 frame + realistic body, or accept you can't detect the failing path.

**Do not proceed until both boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Trust-the-text diagnosis | Blamed replay-fidelity off a log warning + hasty frame read; AI2-THOR state disproved it | blocking | Probe real env metadata before any failure theory |
| Tiny-probe API gating | Small image probe returns 200 while real 143KB request 524s; kill+relaunch hit the same wall 3x | blocking | Probe full-size image payload; or just wait for provider |
| step_id as int / bare 's<idx>' | step_id doubles as PNG filename and fork key; bare ids collide across branches | advisory | Use `_sid(branch,idx)`='<branch>__s<idx>'; treat as opaque; derive parent from `_last_branch_step_id()` |

<current_state>
On branch `session/retrieval-fork-stepid` (pushed to origin, HEAD=49c6081, local==remote). Worktree clean, 72 tests pass. All planned code work for this session is DONE and committed. The ONLY incomplete item is the live E2E verification of the container-retrieval fix — blocked entirely by the provider API (9527code.com) 524-ing on heavy image requests. A wedged resume process (WSL task bay2ahazq) may still be alive; kill it before relaunching.
</current_state>

<completed_work>

- Task 1: Diagnose interrupted heat-task resume — Done (root cause = pose/interaction-visibility, NOT replay-fidelity; proven by AI2-THOR probe)
- Task 2: Container-retrieval Planner prompt fix — Done (3e412de; RETRIEVAL rule in PLANNER_SYSTEM + EXPLORE_BIAS_SYSTEM)
- Task 3: Structured Oracle counterfactual_gold + fork routing (AC→EB CF, PA/WA→Oracle gold) — Done (3e412de)
- Task 4: Globally-unique branch-prefixed step_id via `_sid()` + `_last_branch_step_id()` — Done (3e412de)
- Task 5: E2E verification — In progress, BLOCKED by provider API (see blockers)
- Task 6: Merge PR#3 (partner scheduler rewrite) + resolve conflicts + fix latent fork objectId bug — Done (f4d8051, 49c6081)
</completed_work>

<remaining_work>

- Task 5 (p2-verify): rerun the paused heat episode once API recovers; confirm agent re-approaches Microwave, retrieves Hot Egg, reaches task_complete
- pr-land: open PR session/retrieval-fork-stepid → main (NOTE: branch bundles BOTH partner PR#3 and this session's work; decide combined vs split)
- p3-rest: broader fuzzy intent decomposition (beyond container-retrieval)
- p4: systematic exploration + scan→scan fix
- p5: fork validation E2E (now unblocked by the fork objectId-resolution fix)
- p6: 7-task baseline (5 episodes each)
</remaining_work>

<decisions_made>

- Fixed container-retrieval as a Planner PROMPT change, not a replay_steps reconcile (the reconcile targeted a non-existent bug once the probe proved replay was faithful)
- step_id separator = `__` (double underscore) because step_id IS the PNG filename and `:` is illegal on Windows
- parent_id derived from `_last_branch_step_id()` (reads real episode state) → robust across the old-format resume seam
- Fork forks EB's counterfactual on AC, Oracle's gold on PA/WA → decouples the reasoning grade (label) from a proven recovery path (fork outcome)
- Merged PR#3 into the session branch (no textual conflicts, 72 tests pass); user may split at PR-open time
- Fixed `_run_fork` objectId resolution during the merge (PR#3 enables forks by default; raw objectType would break the new structured-gold fork path)
</decisions_made>

<blockers>
- Provider 9527code.com 524s on the real 143KB image Planner request; 300s socket timeout freezes each attempt ~5min (no console/AI2-THOR output). Workaround: wait (recovers in minutes) OR lower vlm_client.py timeout 300s→60s for fast-fail retries. Verification cannot complete until upstream stabilizes.
</blockers>

## Required Reading (in order)
1. AI2-THOR probe finding (this file's BLOCKING CONSTRAINTS) — why the resume "failure" was misdiagnosed
2. `src/eb_agent.py` PLANNER_SYSTEM + EXPLORE_BIAS_SYSTEM RETRIEVAL rule — the fix under test
3. `src/oracle_agent.py` PHASE4 — structured counterfactual_gold schema
4. `src/branch_runner.py` `_sid` / `_last_branch_step_id` / `_build_fork_task` — step_id + fork routing
5. `.planning/ROADMAP.md` — remaining P3–P6

## Infrastructure State
- Branch `session/retrieval-fork-stepid` pushed to origin (HEAD 49c6081); local == remote; worktree clean
- 72 tests pass (`uv run python -m pytest -q`)
- Paused episode: `output_e2e_20260717_184436/trial_T20190908_215610_893109.json` (heat task, Egg Hot in closed Microwave, 14 steps replayed)
- Possible leftover WSL process (task bay2ahazq) wedged on API — `pkill -9 -f resume_episode.py` before relaunch
- All cron watchers deleted this session

<context>
Highly productive session: turned a stuck resume into a correct root-cause (pose/visibility, via ground-truth probe — caught my own wrong theory), shipped the prompt fix + structured gold + step_id uniqueness (+9 tests), then merged the partner's scheduler PR cleanly and fixed a latent fork objectId bug (+1 test). Everything is committed, tested, and pushed. The single loose thread is a live E2E run that the provider API won't let through — pure external timing, nothing code-side to fix.
</context>

<next_action>
Start with: probe the FULL image path (not a tiny one). If HTTP 200, `pkill -9 -f resume_episode.py`, then relaunch `cd ~/embodied-failure-dataset && export PATH="$HOME/.local/bin:$PATH" && uv run python scripts/resume_episode.py output_e2e_20260717_184436/trial_T20190908_215610_893109.json --api-key sk-IxX... --no-traps --memory semantic`. Watch for `RESP model=` (past the wedge point) and intents with approach/open/pickup Microwave/Egg. Success = agent retrieves the Hot Egg and reaches task_complete.
</next_action>
