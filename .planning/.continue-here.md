---
context: default
phase: m1-p2
task: 7
total_tasks: 7
status: in_progress
last_updated: 2026-07-18T16:12:11+0800
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> Each constraint below was discovered through failure this session. Acknowledge before proceeding.

- [ ] **CONSTRAINT: 30s is the correct VLM timeout for GPT-5.5 image reasoning.** 5s caused false timeouts (model needs 8-20s to generate). 300s froze for 5min on dead API. Changing to any other value without re-verifying will cause unnecessary failures.
- [ ] **CONSTRAINT: Do NOT set VLM timeout below 10s for image calls.** The Planner's image reasoning regularly takes 10-18s. 5s triggered cascading retries that all failed.

**Do not proceed until both boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Aggressive timeout | 5s timeout broke VLM calls that take 8-20s; appeared as "API down" but was actually "too fast to wait" | blocking | Floor timeout at 10s for image calls; use 30s default |
| Trust-the-text diagnosis | Previous session: blamed replay-fidelity off a log warning; AI2-THOR state disproved it | blocking | Probe real env metadata before any failure theory |

<current_state>
Massive bugfix session on scheduler + fork + counterfactual quality. 12 files modified, +325/-86, all uncommitted. On branch f4d8051 (post PR#3 merge). All 14 scheduler tests pass. E2E blocked by API intermittency. Last resume attempt ran 16 successful VLM calls then timed out on Executor. Memory persistence fix works (replay correctly feeds steps to semantic memory) but Planner is still in exploration loop post-resume — doesn't trust memory's Egg location hint over exploration heuristics.
</current_state>

<completed_work>

Round 1 — Scheduler fixes:
- **`_finalize` race condition**: Added `update_final_outcome()` to EpisodeManager (atomic read-modify-write inside `_mutate` lock). `_finalize` now calls it instead of lock-unsafe read-then-write.
- **Dead code removal**: Deleted `self.fork_manager = ForkManager(None, None, None)` and duplicate `self._lock` in scheduler `__init__`.
- **Trap planner pipeline support**: Added `no_traps` to `SchedulerConfig`, `--no-traps` flag to `run_pipeline.py`, `TrapPlanner` created in `_run_branch_worker` and passed to `run_single_branch()`.
- **Fork status overwrite fix**: `_run_fork` no longer sets episode status (only main branch does). Removed except-block status writes.

Round 2 — Fork logic:
- **Action validation (Fix 5)**: `_build_fork_task` validates `alternative_action.action` against `_VALID_ACTIONS - _META_ACTIONS`. Invalid → log warning + return None.
- **Missing fork block in Path A (Fix 1)**: MoveSequence failures (Path A, `branch_runner.py:1268`) now have the same `should_fork` check as single-action failures (Path B). Both paths produce forks.
- **Real rewrite context (Fix 2)**: `_rewrite_reasoning` now receives the replaced step's actual `action` and `eb_reasoning` from `ep.data["steps"]`, not hardcoded `"?"` and `""`.
- **Null gold warning (Fix 3)**: Both Path A and B log a warning when Oracle WA/PA but `counterfactual_gold` is null.
- **MoveSequence in fork**: Added `execute_move_sequence_steps()` standalone function. `_run_fork` now handles MoveSequence alt_actions. Removed MoveSequence ban from prompts and validation.
- **Fork-mid-run parallel**: `_fork_callback` queue mechanism in BranchRunner → scheduler. Forks submitted immediately when created, drained by main loop. `fork_callback` threaded through `run_single_branch`, `BranchRunner.resume`, `_run_fork`.

Round 3 — Prompt fixes:
- **EB Phase 3**: Counterfactual now REQUIRED (was optional). Removed "set to null if unsure" language. `diagnose_failure` retries with CRITICAL REMINDER if null.
- **Oracle Phase 4**: Listed all 18+1 allowed actions explicitly. Forbids Done/LookAround. Added MoveSequence guidance with steps format.

Round 4 — VLM:
- **Timeout**: 300→5→30s (final). Transport attempts 3→5. Server error backoff 1s/2s→0.5s/1s/1.5s/2s/2.5s. Timeout retry increment removed (stays flat).
- **Base URL**: www.9527code.com → cdn.9527code.com (all 7 files: scheduler, 4 scripts, config.toml, CLAUDE.md).
- **Warmup**: Scheduler warmup wrapped in try/except (non-fatal). e2e_test already had this.
- **Worker except**: `_run_branch_worker` returns BranchResult with `worker_crash` + sets "interrupted" instead of re-raising. Auto-retry on next scheduler run.

Round 5 — Memory persistence:
- **replay_steps**: Added `memory` and `task_criteria` params. Calls `memory.update()` after each replayed step (MoveSequence sub-steps included, failed steps included).
- **BranchRunner.resume**: Creates warmed memory before replay, passes to `replay_steps()`, then passes to `runner.run(_memory=...)`.
- **BranchRunner.run**: Accepts `_memory` param; uses it instead of creating fresh memory.

</completed_work>

<remaining_work>

- Task 7: E2E verification. Resume `output_e2e_20260717_184436/trial_T20190908_215610_893109.json`. Agent needs to complete the heat-and-place task. Fork should be created if counterfactual→should_fork fires.
- Potential: Planner prompt tuning for exploration vs memory priority (agent ignores Egg-in-Microwave memory cue).
- Commit and PR this work.

</remaining_work>

<decisions_made>

- 30s timeout is the right value for GPT-5.5 image reasoning (not 5s, not 300s)
- cdn.9527code.com as base URL (same backend, different edge)
- Force counterfactual in EB Phase 3 + retry on null (better to have imperfect CF than none)
- MoveSequence allowed in fork alt_action (added expander, not a real barrier)
- Fork mid-run parallel via queue callback (logically independent of main completion)
- Semantic memory rebuilt during replay (not persisted to disk, rebuilt from step history at resume time)

</decisions_made>

<blockers>
- Provider cdn.9527code.com intermittent timeouts on large image requests (~143KB multimodal Planner calls). 30s timeout catches it quickly but resuming still fails mid-run. Not code-side.
- Post-resume, Planner is stuck in EXPLORATION phase searching random receptacles. Memory correctly shows "Pick up ✓, Heat ✓, Place in CounterTop ✗" but Planner ignores the Egg-in-Microwave cue. May need Planner prompt priority tuning.
</blockers>

## Required Reading (in order)
1. `src/episode_manager.py` `update_final_outcome` — atomic outcome write
2. `src/branch_runner.py` `execute_move_sequence_steps` + fork blocks (lines ~1268, ~1567) + `_fork_callback` usage
3. `src/scheduler.py` `_fork_queue` mechanism + `_on_fork` callback
4. `src/eb_agent.py` PHASE3_SYSTEM (counterfactual REQUIRED section)
5. `src/oracle_agent.py` PHASE4_SYSTEM (allowed actions list)
6. `src/vlm_client.py` timeout=30 + transport retry logic
7. `src/branch_runner.py` `replay_steps` — memory param, `BranchRunner.resume` — warmed memory flow

## Infrastructure State
- Branch: f4d8051 (post PR#3 merge). 12 files modified, uncommitted.
- 14 scheduler tests pass. 72 total tests pass (from previous session).
- Paused episode: `output_e2e_20260717_184436/trial_T20190908_215610_893109.json` (status=interrupted, ~37 steps, heat task)
- API base: cdn.9527code.com/v1 with key sk-IxXjSiRZ3IhCd4hxMajweiXamF0R1U1cHmNECBrRbIz8v0hy

<context>
This session started as a code review of the partner's PR#5 (causal traps)—which turned out to have critical bugs and was rejected. Then shifted to auditing the already-merged PR#3 scheduler, finding 5 confirmed bugs + several design gaps. Fixed them all across 2 rounds. Extended to counterfactual quality (forcing output, validating actions, supporting MoveSequence). Added fork parallelism. Found and fixed the semantic memory persistence gap for resume. The session produced ~325 lines of bugfixes across 12 files. The only loose thread is the E2E verification blocked by API flakiness.
</context>

<next_action>
Start with: `cd ~/embodied-failure-dataset && export PATH="$HOME/.local/bin:$PATH" && uv run python scripts/resume_episode.py output_e2e_20260717_184436/trial_T20190908_215610_893109.json --api-key sk-IxXjSiRZ3IhCd4hxMajweiXamF0R1U1cHmNECBrRbIz8v0hy --no-traps --memory semantic`. Watch for counterfactual→fork creation in logs. Success = task_complete + non-zero fork count in final_outcome.
</next_action>
